import { ApiParamsValidationPipe } from './api-params-validation.pipe';

describe('ApiParamsValidationPipe', () => {
  it('should be defined', () => {
    expect(new ApiParamsValidationPipe()).toBeDefined();
  });
});
